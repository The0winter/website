# 项目协作约定

## Git 工作流

- 用户已明确要求只保留并使用 `main`。后续任务直接在本项目的 `main` 工作，不创建功能分支、`codex/*` 分支或额外 worktree，不使用 Pull Request 作为默认交付流程。
- 开始修改前检查当前分支、工作区和远程状态；保留用户及其他任务尚未提交的改动，不覆盖、不丢弃、不擅自纳入本次提交。
- 每次完成用户要求的修改后，执行与改动相应的检查，审查提交内容，只提交本次任务涉及的文件，然后直接运行 `git push origin main`。用户已经授权该提交及推送流程，无需每次再询问确认。
- 不把密钥、凭据、运行数据或无关的临时产物加入提交。不要使用 `git add .` 或 `git add -A` 把其他任务的改动一并提交。
- 若远程 `main` 有新提交，先获取并整合；解决冲突时保留双方有效改动并重新检查。禁止强制推送或丢弃已有工作来绕过冲突。
- 如果检查或推送失败，继续处理可解决的问题，并如实说明剩余阻碍，不把尚未推送的修改描述为已同步到 GitHub。
- 只有用户之后明确要求时，才改用分支或 Pull Request 流程。
