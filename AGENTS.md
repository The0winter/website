# 项目协作约定

## 界面与功能改动范围

- 用户于 2026-09-20 明确要求：今后提出界面优化或功能改动时，默认指移动端；本次明确指定平台时，以最新指示为准。
- 桌面端是否需要同步改动、采用什么布局与交互，由实际使用场景和用户体验独立判断，不直接照搬移动端。保留并发挥桌面端的空间、鼠标和键盘优势，常用操作应容易发现、大小合适、路径简短，避免为了移动端优化而使桌面端更难用。
- 评估共享组件和样式对两端的影响；移动端需求在移动端验收，影响到桌面端时同时检查桌面体验。常规适配判断自行完成，不反复要求用户确认。

## 后台运行偏好

- 用户允许使用浏览器，但要求不弹窗、不抢焦点、不打断当前工作。自动化、采集与检查默认使用无头浏览器或隐藏的内置浏览器，不自动打开可见浏览器、恢复窗口或调用聚焦操作。
- 登录或验证码需要用户操作时，先保留进度并提示；只有用户主动要求或点击“显示采集窗口”后才显示。启动后台进程时隐藏终端窗口。
- 用户于 2026-09-21 再次明确要求：测试、构建、部署和辅助命令也必须后台执行，不弹出黑色命令窗口、不抢焦点。检查启动器及其子进程：PowerShell 使用 `Start-Process -WindowStyle Hidden`，Node 使用 `windowsHide: true`，Python 在 Windows 使用 `creationflags=subprocess.CREATE_NO_WINDOW`；仅重定向输出不能代替隐藏窗口。日志留在任务目录，按需读取。

## 小说采集选源偏好

- 用户于 2026-09-14 明确要求将以下规则用于今后的新书采集、换源和续更：按当前这本书的实际质量选源，不固定使用速读谷或任何单一网站。
- 先保质再保量：找不到最新章节或优质来源更新较慢可以接受。不能因目录更新、更大章号或更多章节，优先选取字数异常少、正文残缺、乱码或错字密集的低质量版本；重点核实最新章节实际正文，宁可保留质量更好的版本和已验证的可读进度。
- 当前来源质量不够好时，先查找拾页现有的适用网站并试采比较；仍无足够好的版本，再到外部寻找来源，必要时适配新站。合理时间内找不到完美版本，就选择质量相对高、可读且能核实作品身份和顺序的版本继续采集，不因已知的来源瑕疵反复请求确认。
- 遇到问题先自行排查；验证码或访问失败有限重试后换源或保留进度跳过，确实需要用户才能解决时再询问。不得编造缺失正文或将验证页面当作正文。
- 用户要求交付报告只简短列出“书名 + 绑定的网站 + 缺失章节或有问题的章节及其问题”，例如“《书名》：绑定速读谷；第 123 章缺失；第 456 章末尾缺文”。没有已知问题的书也列出绑定网站，问题部分简写“未发现章节问题”。网站以实际保存的绑定为准；未绑定或无法确认时如实标明，不把候选网站当作已绑定。不附选源理由、数量、采集终点、检查过程等其他信息，除非用户另问。详细记录保留本地。具体流程见 [智能找书与采集](docs/智能找书与采集.md) 的“长期选源规则”。用户之后针对某本书的明确指定或跳过要求优先。

## 监督任务的执行成本

- 用户于 2026-09-20 明确要求记录：监督更新等批处理任务默认采用低 token 开销的执行方式，在保证采集质量和稳定性的前提下控制模型介入范围。
- 脚本负责运行、完整数据比对、检查点和成功项验收；模型只读取精简状态及异常所需证据。完整日志、目录、正文和差异保存在本地，默认仅输出一屏结构化摘要；输出被截断时收窄查询，不重新输出全量内容。
- 复用已有工具、检查点和核对记录，按异常类型集中处理，避免逐书重复读大段上下文、重复编写临时工具或无新依据地重跑检查。进度说明简短，聚焦新发现、决定和剩余阻碍。
- 常规可解决的代码问题仍自主修复；复杂来源问题有限核对后保留原版、记录并继续其余书籍，再说明额外调查的范围，不把一次常规监督默认扩展成大规模重采或数据修复。不得以节省 token 为由降低质量检查、放宽保护或把暂缓项算作成功。具体流程见 [智能找书与采集](docs/智能找书与采集.md) 的“监督更新与 token 成本控制”。

## 本地测试临时目录

- 测试、隔离开发和基准验证的临时文件统一放在项目内的 `.runtime/test-tmp/`。新增测试入口先加载 `tools/test-env.cjs`；临时验证命令使用 `node --require ./tools/test-env.cjs ...`，让数据库、浏览器及子进程继承项目内的 TEMP/TMP/TMPDIR。
- 浏览器测试使用 `npm run test:browser`，在 Playwright 加载前设置编译缓存目录。临时数据继续按各自测试的生命周期清理，不写入 Git，也不修改 Windows 全局临时目录。

## 线上浏览器验收的统计隔离

- 线上浏览器验收必须在首次导航前屏蔽 Google Analytics 脚本及上报，避免独立浏览器上下文和设备模拟被计为新访客。Playwright 测试优先使用 `web-next/tests/fixtures/without-analytics.ts` 的 `test`；另建上下文时调用其中的 `blockAnalytics(context)`。Puppeteer 等工具同样拦截谷歌统计域名；仅拦截本站阅读次数接口不能隔离 GA。
- 此规则只隔离我们主动发起的验收流量，不修改真实用户浏览器，不按城市或短停留批量删除历史统计。历史真人与测试访问无法逐个归因时，报告限制，不强行把标识数合并为自然人数。

## 本地容量与清理

- 新任务使用固定的日常构建目录；额外 `NEXT_DIST_DIR` 仅用于隔离验证。构建、测试优先使用根目录 npm 入口，它们会运行本地清理并登记活动保护；直接测试仍须加载 `tools/test-env.cjs`。
- 清理统一使用 `npm run storage:plan` 预览、`npm run storage:clean` 执行，规则见 [本地容量与清理](docs/本地容量与清理.md)。不得直接清空 `.runtime`、`.novel-crawler`、`artifacts` 或测试临时根目录。
- 调试截图、日志统一放入 `.runtime/task-artifacts/<任务>/`，使用 `debug`、`initial`、`before` 等明确文件名；最终验收材料使用 `final`、`verified` 等标记。需要临时保留某个目录时放置 `.storage-keep` 文件，任务结束后检查是否仍需保留。
- 清理只淘汰可再生成的输出。小说、封面、续更映射、原始取证页面、登录数据、恢复备份和不明用途的任务快照不得按时间或总容量直接删除。以后新增一次性工具应给临时产物明确生命周期，不再创建无上限的构建/安装副本。
- 2026-09-26 起，监督更新使用书库入口自带的按书压缩去重快照，不再在任务脚本中复制整库 downloads、jobs 或 continuations。历史备份先用 `npm run storage:snapshot -- archive <已完成任务的backup或before目录>` 完整归档与恢复校验，再由统一 storage:plan / storage:clean 删除未压缩副本；不得绕过归档验证直接删备份。恢复到新的 `.runtime/test-tmp/restored-*` 目录核对，不覆盖运行中的书库。每日隐藏清理与容量记录见 `npm run storage:schedule` 和 `docs/本地容量与清理.md`。

## Git 工作流

- 用户已明确要求只保留并使用 `main`。后续任务直接在本项目的 `main` 工作，不创建功能分支、`codex/*` 分支或额外 worktree，不使用 Pull Request 作为默认交付流程。
- 开始修改前检查当前分支、工作区和远程状态；保留用户及其他任务尚未提交的改动，不覆盖、不丢弃、不擅自纳入本次提交。
- 每次完成用户要求的修改后，执行与改动相应的检查，审查提交内容，只提交本次任务涉及的文件，然后直接运行 `git push origin main`。用户已经授权该提交及推送流程，无需每次再询问确认。
- 不把密钥、凭据、运行数据或无关的临时产物加入提交。不要使用 `git add .` 或 `git add -A` 把其他任务的改动一并提交。
- 若远程 `main` 有新提交，先获取并整合；解决冲突时保留双方有效改动并重新检查。禁止强制推送或丢弃已有工作来绕过冲突。
- 如果检查或推送失败，继续处理可解决的问题，并如实说明剩余阻碍，不把尚未推送的修改描述为已同步到 GitHub。
- 只有用户之后明确要求时，才改用分支或 Pull Request 流程。

## 部署工作流

- 用户已明确授权：网站修改完成并通过相应检查、提交和推送后，直接部署到现有网站 `https://jiutianxiaoshuo.com`，不再逐次询问部署确认。
- 部署前核对线上版本与本次提交，保留已上线功能和现有服务器配置；不能用缺少线上功能的仓库版本直接覆盖服务器。已有未提交改动仍按上面的 Git 约定保留。
- 在新的版本目录准备代码并完成构建，保留当前兼容版本用于回滚，再切换 `/srv/test1/current` 并重启必要服务。部署后验证服务、健康检查、页面、目录及章节正文，确认公网生效后再报告完成。
- 部署失败时处理问题或回滚到上一兼容代码版本，如实说明状态。常规部署不包含破坏性数据库变更、秘密更换或删除业务数据。
- 部署成功后只保留当前版本和最近两个已验证、与现有数据兼容的回滚版本。新版本的 `deployment-manifest.json` 必须记录 `sourceCommit`、`release`、`previousRelease`，并在验收成功后写入 `activatedAt`；验收失败不写成功标记。
- 服务器通过 `test1-release-prune.path` 在版本切换后自动清理，另有每日 timer 兜底；部署验收后可执行 `sudo systemctl start test1-release-prune.service` 并核对结果。清理脚本为 `infra/release_retention.py`，默认仅预览，保留尚未完成部署或仍被进程使用的目录，绝不清理 `/srv/test1/backups`、数据库、R2 对象和 `/etc/test1` 配置。若保留条件或健康检查不满足，先处理原因，不绕过保护强行删除。

## 封面上传流程

- 所有书籍封面统一使用 `infra/upload-cover.mjs`，按书名（可附作者）或书籍 ID 以及图片路径传参；先预览核对，用户已授权上传时执行 `--apply`。用法见 `docs/R2封面存储.md`。
- 不再为单本书或单张图片新建上传、数据库绑定、SSH 调用脚本。`.runtime/` 只保存该次操作的数据与结果；需要扩展能力时修改通用工具。仅上传封面不需要部署或重启网站。
