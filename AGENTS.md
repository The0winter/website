# 项目协作约定

## 后台运行偏好

- 用户允许使用浏览器，但要求不弹窗、不抢焦点、不打断当前工作。自动化、采集与检查默认使用无头浏览器或隐藏的内置浏览器，不自动打开可见浏览器、恢复窗口或调用聚焦操作。
- 登录或验证码需要用户操作时，先保留进度并提示；只有用户主动要求或点击“显示采集窗口”后才显示。启动后台进程时隐藏终端窗口。

## 小说采集选源偏好

- 用户于 2026-09-14 明确要求将以下规则用于今后的新书采集、换源和续更：按当前这本书的实际质量选源，不固定使用速读谷或任何单一网站。
- 先保质再保量：找不到最新章节或优质来源更新较慢可以接受。不能因目录更新、更大章号或更多章节，优先选取字数异常少、正文残缺、乱码或错字密集的低质量版本；重点核实最新章节实际正文，宁可保留质量更好的版本和已验证的可读进度。
- 当前来源质量不够好时，先查找拾页现有的适用网站并试采比较；仍无足够好的版本，再到外部寻找来源，必要时适配新站。合理时间内找不到完美版本，就选择质量相对高、可读且能核实作品身份和顺序的版本继续采集，不因已知的来源瑕疵反复请求确认。
- 遇到问题先自行排查；验证码或访问失败有限重试后换源或保留进度跳过，确实需要用户才能解决时再询问。不得编造缺失正文或将验证页面当作正文。
- 用户要求交付报告只简短列出“书名 + 绑定的网站 + 缺失章节或有问题的章节及其问题”，例如“《书名》：绑定速读谷；第 123 章缺失；第 456 章末尾缺文”。没有已知问题的书也列出绑定网站，问题部分简写“未发现章节问题”。网站以实际保存的绑定为准；未绑定或无法确认时如实标明，不把候选网站当作已绑定。不附选源理由、数量、采集终点、检查过程等其他信息，除非用户另问。详细记录保留本地。具体流程见 [智能找书与采集](docs/智能找书与采集.md) 的“长期选源规则”。用户之后针对某本书的明确指定或跳过要求优先。

## Git 工作流

- 用户已明确要求只保留并使用 `main`。后续任务直接在本项目的 `main` 工作，不创建功能分支、`codex/*` 分支或额外 worktree，不使用 Pull Request 作为默认交付流程。
- 开始修改前检查当前分支、工作区和远程状态；保留用户及其他任务尚未提交的改动，不覆盖、不丢弃、不擅自纳入本次提交。
- 每次完成用户要求的修改后，执行与改动相应的检查，审查提交内容，只提交本次任务涉及的文件，然后直接运行 `git push origin main`。用户已经授权该提交及推送流程，无需每次再询问确认。
- 不把密钥、凭据、运行数据或无关的临时产物加入提交。不要使用 `git add .` 或 `git add -A` 把其他任务的改动一并提交。
- 若远程 `main` 有新提交，先获取并整合；解决冲突时保留双方有效改动并重新检查。禁止强制推送或丢弃已有工作来绕过冲突。
- 如果检查或推送失败，继续处理可解决的问题，并如实说明剩余阻碍，不把尚未推送的修改描述为已同步到 GitHub。
- 只有用户之后明确要求时，才改用分支或 Pull Request 流程。

## 部署工作流

- 用户已明确授权：网站修改完成并通过相应检查、提交和推送后，直接部署到现有网站 `https://jiutianxiaoshuo.com`，不再逐次询问部署确认。
- 部署前核对线上版本与本次提交，保留已上线功能和现有服务器配置；不能用缺少线上功能的仓库版本直接覆盖服务器。已有未提交改动仍按上面的 Git 约定保留。
- 在新的版本目录准备代码并完成构建，保留当前兼容版本用于回滚，再切换 `/srv/test1/current` 并重启必要服务。部署后验证服务、健康检查、页面、目录及章节正文，确认公网生效后再报告完成。
- 部署失败时处理问题或回滚到上一兼容代码版本，如实说明状态。常规部署不包含破坏性数据库变更、秘密更换或删除业务数据。
- 部署成功后只保留当前版本和最近两个已验证、与现有数据兼容的回滚版本。新版本的 `deployment-manifest.json` 必须记录 `sourceCommit`、`release`、`previousRelease`，并在验收成功后写入 `activatedAt`；验收失败不写成功标记。
- 服务器通过 `test1-release-prune.path` 在版本切换后自动清理，另有每日 timer 兜底；部署验收后可执行 `sudo systemctl start test1-release-prune.service` 并核对结果。清理脚本为 `infra/release_retention.py`，默认仅预览，保留尚未完成部署或仍被进程使用的目录，绝不清理 `/srv/test1/backups`、数据库、R2 对象和 `/etc/test1` 配置。若保留条件或健康检查不满足，先处理原因，不绕过保护强行删除。

## 封面上传流程

- 所有书籍封面统一使用 `infra/upload-cover.mjs`，按书名（可附作者）或书籍 ID 以及图片路径传参；先预览核对，用户已授权上传时执行 `--apply`。用法见 `docs/R2封面存储.md`。
- 不再为单本书或单张图片新建上传、数据库绑定、SSH 调用脚本。`.runtime/` 只保存该次操作的数据与结果；需要扩展能力时修改通用工具。仅上传封面不需要部署或重启网站。
