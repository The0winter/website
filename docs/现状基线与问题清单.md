# 现状基线与问题清单

## 数据范围
用户确认旧 VPS 不保留。独立数据库、快照、异机备份未知。仅本地小说 JSON 有已验证来源：41 本/29,188 章；无旧书 ID、用户、密码哈希、书架、评价、论坛恢复证据。保留源文件，生产重建尚未授权。
历史配置位于忽略的 .env、server/.env、web-next/.env；不加载、不复制入 Git。历史配置中既有回环也有远端数据库目标，变量名称不能证明隔离。

## 环境基线
锁文件：根 Next 16.1.6/React 18.3.1/Express 5.2.1/Mongoose 9.1.5；前端 Next 16.1.6/React 19.2.3；后端 Express 4.22.1/Mongoose 8.22.0。
页面、API、脚本入口清单见 artifacts/source-inventory.json。所有计划 SEC/REL/DATA/UX/SEO/ENG 项初始未修复。没有运行截图，不声称保真回归通过。

## 新发现
- 创作页面新章号恒为1、编辑请求携带bookId：随唯一索引和字段白名单修复调用方；待创作E2E。
- 详情页字数依赖未返回的正文导致0字，改读word_count。
- 旧源码中的署名自动创建账号、按书名导入和在线清章危险，替换为稳定来源导入与只读盘点。
- 原始截图实际已移入 artifacts/baseline；390首页已人工查看，其他尺寸/页面未全部人工复核。
- R1 lint仍102 errors/77 warnings；依赖审计前端20、后端22项（包括高危），尚未逐项评估/补丁；不能发布。
