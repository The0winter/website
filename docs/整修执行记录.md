# 整修执行记录

## 2026-09-07 · R0 开始
- 基线 HEAD：8fe89c9e1319e12917d53ae424cc77860523afe8；开始时只有 run_update.js 修改和未跟踪 docs/计划。
- 用户文件 SHA-256：2817172195b8c45e59c94bdc1738b340424fe9b7bf4390003f961a90655073db；不得覆盖。
- 没有既有执行记录。按 R0→R6 继续，不把本地阶段通过视为全站通过。
- 用户确认旧 VPS“不保留”；独立数据库/异机备份尚无证据。未连接任何既有数据库，未运行旧脚本。
- downloads：41 个 JSON 全部解析成功，29,188 章，390,328,334 字节；0 个文件含顶层 id/_id。不是完整业务备份。
- 本地 .env、server/.env、web-next/.env 含历史配置；只检查键名及目标类别，未打印秘密，不自动加载这些文件。
- 当前 Node 20.20.0/npm 10.8.2；Docker CLI 已安装，daemon 未启动；没有 PATH mongod/mongodump。
- 当前包 R1-01：分离 app/启动，显式隔离配置，默认关闭外发，建立真实 MongoDB 副本集测试入口。

## 阶段状态
|阶段|状态|
|---|---|
|R0|数据文件盘点完成；旧业务数据/ID恢复未证实；视觉基线待可运行环境|
|R1|执行中|
|R2—R6|尚未验收；没有公网发布和生产数据变更|

## R1-01/02 检查点
- Node 22.23.2 独立安装在忽略的 .runtime；MongoDB 7.0.14 真实单节点副本集已启动。新增 mongodb-memory-server 只管理测试 mongod，不模拟数据库。
- app 导入无连接/监听/cron；启动等待库、关闭自动索引、有界超时与关闭；默认外发关闭。
- 配置/离线健康检查 2/2 通过（artifacts/r1-config-tests.txt）；typecheck 通过，build 通过但 sitemap 有已记录错误，不能视为有效 sitemap；lint 历史错误见 artifacts/r1-lint.txt。
- Playwright 三宽度首轮访问通过；截图 artifacts/baseline（仅页面访问，不等于操作闭环/人工视觉验收）。
- 下一包 R2 权限与账户；R1 干净目录复现、SMTP 接收器和完整无外发检查仍待补全。

## R2 / R3 实现检查点（尚未整阶段验收）
- 新增 security、auth、media、content、import 模块；保留页面结构与旧公共路径，禁用影子登录与破坏性在线清理。
- HttpOnly Cookie + csrf-csrf 签名双提交 + 精确 Origin；会话集合撤销；账户状态实时读取；密码改动撤销会话；验证码摘要、次数及原子消费。前端统一 same-origin 请求与 CSRF。
- 真实 MongoDB 测试 9/9 通过（artifacts/r3-tests.txt）：匿名/他人写入无变化、字段越权、退出/封禁/解封旧 Cookie、注册/改密、验证码重用、公开隐私、HTML 清洗、并发同号重试/不同正文冲突、并发额度与评分、软删除/恢复。停测试 mongod 时有驱动 ECONNRESET 输出，测试本身通过，需检查退出噪声。
- 媒体新实现：校验实际 JPG/PNG/WebP、字节/像素限制、转码；存 MongoDB Media 字节与 hash，测试不连接 Cloudinary。旧云图继续可引用显示，但归属未确认不能销毁。引用并发、图片浏览器流程仍待补测。
- R3 迁移 inspect 默认只读；不自动删冲突/索引；生产拒绝运行此本地工具。导入仅专用 IMPORT_SECRET 与 sourceUrl，无同名自动账号认领；新 infra/import-local.mjs 支持 dry-run、批次、有限重试和检查点。旧文件/旧 ID 未恢复，存量迁移未验收。
- run_update.js SHA-256 与开始完全一致。

## 当前包 R4
- 数据库分页/排序/转义搜索，目录元数据每次最多200项；前端目录分批加载，搜索分页和排行定向查询改造中。
- 正文 GET 不计数；阅读可见10秒后独立上报，MongoDB 日去重/日汇总，旧累计量保留。日周月排名的任务接续仍待完成。
- SSR 书章错配、不存在与依赖错误已区分；JSON-LD 转义修复；正文缓存20章、书缓存3本。JS关闭导航、快切/返回及全流程待浏览器验收。
- 已通过最新 typecheck；R2 账户 E2E 正在调试（测试选择了桌面隐藏的手机退出按钮，修正可见按钮定位后复跑）。未宣称 R4 完成。

## 2026-09-08 · R4 补齐与 R5 首轮证据（阶段未全部验收）
- 检查点 11334e6 保留旧 Next/Express/Mongoose 应用；run_update.js 哈希再次确认与开始一致。
- 账户浏览器登录/刷新/退出、错误验证码两个 E2E 已通过；首次退出暴露重定向竞态，已修复。最新类型检查通过；上一轮 lint 为 0 错误、81 警告，未关闭检查。
- MongoDB 7.0.40 隔离测试最新 13/13 通过（artifacts/security-reading.txt）：新增有效 Cookie 身份、同账户跨 visitor 并发阅读去重、GET 不计数、统计任务互斥及自然月份归零，累计量保留。论坛可选身份移除旧 Bearer 识别，个性化响应不缓存。
- 统计任务改为全局租约，并在同一事务锁定租约和写入书籍计数，防止失去租约的旧任务覆盖新结果；日聚合保存62天，覆盖自然月/周窗口。迁移入口显式加载任务索引。
- 首页分类改为服务端按分类分页，推荐榜由全库综合分查询；封面保留同源相对路径，避免生产浏览器指向自己的127.0.0.1。新增 sitemap XML 分片待最新构建/浏览器验证。
- R5 首轮负载：Windows本机 nginx→Next生产构建/API→真实单节点MongoDB；1,000书/100,000章，最大章60,000汉字。20 RPS持续1,800秒，共36,000请求，全部200，跳过0；正文/SSR/列表p95分别39.61/49.76/87.93ms。原始报告 artifacts/load-baseline.json。仅代表当时启动的快照、合成数据和该机器，不代表最新提交或生产容量。
- R5 恢复：mongodump压缩归档恢复到另一空MongoDB；集合数量、索引定义、抽样ID/正文hash和恢复API就绪/长章读取通过（artifacts/restore-report.json）。耗时4.615秒，归档1,988,676字节；是同机合成数据恢复，不是异地备份或生产RTO证明。初次索引比较仅因返回顺序不同失败，保留证据后按索引名规范化重跑，未忽略索引定义。
- 仍待：完整写功能/阅读浏览器矩阵，作者与后台分页，导入混合负载及过载恢复、故障演练、干净环境复现、依赖风险收敛、部署/监控/回滚候选和发布影响清单。R0—R6尚未整体验收，不已发布；原用户/书架/评论/旧ID没有恢复证据。

## R3/R4 补齐检查点 · 2026-09-08
- 论坛发帖、回答、两层评论改为事务，重复并发不落第二条、不留下错误计数；点赞原子计算成员集合与计数，前端提交明确目标状态；Cookie 个性化读取不缓存。
- 导入新增真实库反例：dry-run 不写、同名不认领、重复批次保留ID、冲突整批回滚、下架内容须显式恢复；章节恢复接口校验父作品归属。导入本地checkpoint不能证明目标仍有数据，因此重跑时重放幂等批次验证，不直接按偏移跳过。
- 本轮最新后端17项通过（artifacts/security-current.txt）；含 SMTP真实回环接收、临时失败及外部地址拒绝、论坛并发、导入与幂等键、活动汇总。管理员活跃分定义为含今天最近7个上海自然日：阅读1分、上传50分；公开榜单使用上海自然日/周一开始周/月。旧stats与累计量保留版本快照，不清原上传额度。
- 首页/作者/作品工作台分页；作者页面未核实的旧占位成就不再伪装成真实数据。账户与25作品分页E2E三项通过（artifacts/e2e-pages.txt），受控作品由测试软删除。
- 阅读E2E三项通过（artifacts/e2e-reading.txt）：JavaScript关闭时正文可见且上下章可点击；sitemap分片和缺失路径；100次键盘切章+返回，URL/标题一致。首次分别发现不必要Suspense隐藏无脚本正文、键盘effect更新过晚，修复后重跑通过，失败证据保留。内存采样在artifacts/reader-100-switches.json，不能单凭采样声称不存在泄漏。
- 后端依赖删除未使用的旧Cloudinary适配器与Puppeteer（根采集环境未动）；Nodemailer明确升级9.0.1，qs覆盖6.16.0；前端仅锁文件范围内修复，未用audit fix --force。两包当次npm审计均0已知漏洞，不等于安全验收完成。参考安全公告 GHSA-p6gq-j5cr-w38f、GHSA-4mjr-xmp4-gh2g。
- 干净目录安装、17项后端测试、typecheck、lint、无API在线条件的build通过；精确文件hash/命令/目录见artifacts/clean-room-report.json。该快照早于最后的无脚本导航及用户日汇总修改，最终候选需补增量验证与干净启动。
- 生产新增默认只读写开关、HTTPS Origin与非占位密钥检查，代理仅可显式信任loopback；普通JSON256KB，只有专用导入允许10MB。此配置变更仍需最新测试。
- Docker Desktop已尝试启动，但Linux engine命名管道仍不存在；容器实跑未验证。继续本机等效预发布，部署/故障/混合负载/全功能浏览器尚有缺口；没有公网发布或生产写入。
