# 书籍简介同步记录

## 2026-09-11

按用户要求为当前网站已有书籍补全简介，并同步本地下载文件。目标是 `jiutian_prod` 中 44 本已上架的导入书籍，按现有书籍 ID 和 `sourceUrl` 核对，不新建书籍。

- 43 本原为“暂无简介”，已使用原来源详情页的简介补全。
- 《从水猴子开始成神》已有完整简介；本次原来源文本与现有版本存在差异，保留现有 198 字符版本。
- 44 本原来源均访问成功：69 书吧 40 本、搬山人 2 本、爱下电子书 1 本、速读谷 1 本。沿用拾页适配器，逐本核对完整书名和作者，保留简介段落，去除站点关键词等控件文本。
- 《我在俄国当文豪》的旧 `sourceUrl` 是目录地址，本次从同一书籍 ID 的 `.htm` 详情页读取简介，线上来源映射保持原值。

通过现有 `/api/admin/upload-book` 接口提交简介和空章节数组，先执行 dry-run，再正式同步。导入凭据仅由服务器读取。同步前备份位于 `/srv/test1/backups/descriptions-LGCIkW/`，包含原书籍记录、更新计划和逐本结果。

44 本目标书籍的 31,331 条章节记录在更新前后逐条计算 SHA-256，数量和总哈希一致；书籍总数、ID、书名、作者、来源、封面、分类及完结状态保持不变。44 个公网书籍接口均返回预期简介，44 个详情页均显示对应文本；两页首次访问仍显示缓存，触发刷新后复核通过。

本地对应的 44 份 `downloads/` JSON 同步写入简介，并验证除简介外的全部字段保持一致。4 份由拾页管理的导出文件在原哈希核对通过、取得任务锁后更新，同时维护导出哈希和保存的简介，以继续使用已有检查点。

本次没有请求章节正文，也没有部署或切换网站代码。采集响应、来源证据、旧简介、本地修改记录和公网验证结果保存在已忽略的 `.novel-crawler/description-refresh/`，不提交书籍数据或凭据。
