# 网站整修候选验收报告

日期：2026-09-09。交付范围按用户最新要求收敛为现有网站的架构、安全、稳定性、功能完善。旧数据恢复、迁移和导入已取消，数据由用户之后重新上传；无原库连接或生产变更。

候选代码提交：28cc30d，分支codex/legacy-site-candidate。后续报告提交仅补验证证据。用户run_update.js未修改、未提交；原计划保留未跟踪状态。

## 本地运行

在项目根目录执行`.\.runtime\node-v22.23.2-win-x64\node.exe server/dev.js`，访问http://127.0.0.1:3000。该入口新建独立合成副本集，不加载既有.env，不读取旧数据，邮件仅本地捕获。受控账号reader@example.test / Local-test-12345，管理员admin@example.test / Admin-test-12345，仅供本地测试，不能用于生产。退出使用Ctrl+C；重启重新创建合成内容。

## 候选内容

保留Next/Express/Mongoose和现有页面、交互与公共书章路径，不使用Test2。账户改为可撤销HttpOnly Cookie会话与CSRF；写入口检查身份、归属与字段。草稿独立私密保存，发布事务化；书章、评分、论坛计数和任务租约处理并发与幂等。书架、目录、评价、作者和管理列表有服务端分页。正文支持无JavaScript阅读，快速切章校验响应，预取可取消，缓存有上限。

运行入口不自动加载历史.env，不随应用启动旧脚本；隔离服务默认关闭外发。候选提供健康检查、脱敏请求日志、私有指标、限流、维护页和systemd/nginx配置。生产默认只读，公开部署未执行。

## 实测证据

|项目|结果与证据|
|---|---|
|后端真实MongoDB集成测试|最新27/27通过；包含28个受保护写入口的匿名伪造身份反例、归属、并发草稿/章号/评分/论坛、会话、合成上传、配置/故障/监控规则。artifacts/security-final.txt|
|完整浏览器回归|16/16通过：注册登录刷新退出、管理员封禁解封、头像改密、创作、草稿、论坛、书架评分、分页、阅读、三宽度访问。artifacts/e2e-final.txt|
|阅读缓存与导航|30章/5本书实测Map始终不超过20章/3本书；100次切章和浏览器返回、无JS正文导航通过。artifacts/cache-browser-report.json、web-next/artifacts/reader-100-switches.json|
|干净安装与构建|独立目录按锁文件安装，后端测试、typecheck、lint、无API在线条件build通过。精确代码hash和命令见artifacts/clean-room-report.json；不关闭类型检查，lint保留既有警告|
|生产Cookie与维护页|真实nginx回环HTTPS及干净Next生产构建；Secure/HttpOnly/SameSite/退出重放验证通过，候选→维护503→同候选恢复757ms。artifacts/production-cookie-report.json、maintenance-recovery-report.json|
|查询计划|万章目录返回100项只扫描100项，正文扫描1项。artifacts/query-plans.json|
|故障与监控|真实数据库中断/恢复、API重启、任务真实租约过期接管通过；本地监控报告故障/备份超龄，不外发。artifacts/runtime-faults.txt、process-recovery-report.json、monitor-candidate-report.json|
|通用备份能力|此前用少量合成账号/书架/评分/草稿验证归档及空实例恢复与登录，非旧数据恢复。artifacts/backup-candidate-report.json；按用户要求本轮不再处理旧数据|

## 性能证据边界

本机Windows、Node22.23.2、MongoDB7.0.40单节点副本集、nginx1.30.4、Next生产构建；1000本/100000章合成数据，含万章目录和60000汉字长章。各负载报告有启动代码hash，期间存在同机开发/构建干扰，不据此推算线上用户数或目标服务器容量。

- 完整基准1800秒、20RPS、36000请求全部200，正文/SSR/列表p95分别42.28/57.61/79.37ms：artifacts/load-candidate-baseline.json。
- 首次完整混合负载35999个200、1个502，nginx日志显示Windows上游连接地址耗尽；据此增加上游keepalive。原始失败保留于artifacts/load-candidate-mixed.json、mixed-nginx-error.txt。
- keepalive后的30分钟复跑中断于约第25分钟，没有完整报告，不计通过。后续300秒混合回归6000请求全部200、无跳过，正文/SSR/列表p95为21.50/50.32/34.08ms；该短测不能替代完整30分钟混合稳定性验收。artifacts/load-candidate-keepalive-short.json。
- 40RPS持续180秒，7200请求全200、无跳过；100RPS持续180秒，18000请求中12600个200、5400个429，无5xx/超时/跳过。限流样本不能用于宣称成功响应延迟提升；此处测的是API按IP限流，生产nginx额外50RPS限制尚未在目标主机验证。artifacts/load-candidate-overload-40.json、load-candidate-overload-100.json。
- 降回20RPS持续120秒，2400请求全200，无5xx/超时/跳过；最后一分钟1200请求也全200。artifacts/load-candidate-overload-recovery.json。负载测试服务已停止，本地合成开发预览保留。

## 外观与未验证项

保留首页、论坛、详情及阅读布局；分页与草稿控件是必要的局部变化。15张三宽度访问截图在web-next/artifacts/current，基线在artifacts/baseline；已人工对照部分首页/论坛/详情/阅读。未逐张逐状态完成视觉验收；未登录创作页基线仅加载状态，不能据此证明完整工作台保真，工作台功能由E2E验证。

目标Linux开机启动、systemd沙箱与定时任务、公网证书/DNS、真实邮件、异机备份与外部告警渠道尚未验证。维护页切换已测，跨版本/跨数据库结构回滚未测。没有执行公网发布、24小时或7天观察，也没有安排自动回访。

## 未来发布影响（本次不执行）

需要明确新主机、域名、空数据库及独立秘密；初始化模型索引，安装服务并配置证书和代理。先只读公开阅读，再有序启用账户/创作/论坛写入；启用真实邮件、任务和用户自行上传数据均应明确配置。不会把测试账号或合成内容搬上生产，不启动run_update.js或其他旧修库任务。

具体命令与维护切换路径见部署备份与恢复手册。没有公网目标和发布授权时，本候选作为本地交付；不把部署模板存在或阶段测试通过视为已上线。
